" Indents are smart, length 4 and insert space characters
set tabstop=8
set softtabstop=0
set expandtab
set smarttab
set shiftwidth=4

" Display line numbers
set number

" Color scheme
colo slate

" Show editing mode
set showmode

" Tell vim to keep a backup file
set backup

" Backup directory
set backupdir=/tmp/

" Swap file directory
set dir=/tmp/

" Highlight matching search strings
set hlsearch

